{
  "name": "agent-control-plane-deployment",
  "private": true,
  "version": "0.0.0",
  "description": "Panel behavior tests for the deployment control plane web UI",
  "scripts": {
    "test": "node --test web/app.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
